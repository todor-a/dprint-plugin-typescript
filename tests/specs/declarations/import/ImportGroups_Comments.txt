~~ {
  "lineWidth": 80,
  "module.sortImportDeclarations": "maintain",
  "module.importGroups": [
    { "match": "builtin" },
    { "match": "external" },
    { "match": ["sibling", "index"] }
  ]
} ~~
== trailing line comment stays with the import it trails ==
import { a } from "react"; // trails react
import { fs } from "node:fs";

[expect]
import { fs } from "node:fs";

import { a } from "react"; // trails react

== trailing block comment stays with the import it trails ==
import { a } from "react"; /* trails react */
import { fs } from "node:fs";

[expect]
import { fs } from "node:fs";

import { a } from "react"; /* trails react */

== dprint-ignore-start markers stay with their imports (region not honored, see README limitations) ==
// dprint-ignore-start
import { a } from "react";
import { fs } from "node:fs";
// dprint-ignore-end

[expect]
import { fs } from "node:fs";

// dprint-ignore-start
import { a } from "react";
// dprint-ignore-end
